# Local environment
.env

# Go workspace (for local multi-module dev; see inventar ASR-0008)
go.work
go.work.sum

# Build artifacts (top-level compiled binaries; cmd/oo/ source tree is tracked)
/oo
/oo-cli
/bin/
